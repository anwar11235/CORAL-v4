# CORAL v4.2 — Phase 3a: Simple crystallisation (monolithic codebook, no disentanglement)
# Goal: verify that crystallisation integrates cleanly with the full forward path before
# enabling multi-head disentanglement. Uses a single codebook head with 256 entries.
# Mode: "full" — PC disabled, crystallisation enabled, single-head codebook.

model:
  # Single level: d=512
  n_levels: 1
  level_dims: [512]

  # Backbone
  backbone_layers: 2
  backbone_dim: 512
  n_heads: 8
  d_k: 64
  ffn_expansion: 4

  # Timescale: T=3 inner steps per segment
  timescale_base: 3

  # Predictive coding — OFF in phase 3 (crystallisation-first research order)
  use_predictive_coding: false
  lambda_pred: 0.001
  lambda_pi: 0.001   # deprecated in v4.2, kept for compat
  epsilon_min: 0.01

  # Halting
  K_max: 16
  halting_threshold: 0.95
  halting_exploration_prob: 0.1
  halting_gamma: 0.9

  # v4.2 mode flag
  mode: "full"

  # Crystallisation — monolithic (1 head, 256 entries)
  use_crystallisation: true
  codebook_heads: 1
  codebook_entries_per_head: 256
  tau_converge: 0.01
  tau_decrystallise: 0.05
  n_stable: 2
  lambda_commit: 0.25
  lambda_dis: 0.0   # no disentanglement loss in phase 3a (single head)
  lambda_crystal: 0.0

  # No amortisation in phase 3
  use_amort: false
  lambda_amort: 0.0

  # Other components
  use_column_heads: false
  use_stochastic: false

  # Precision
  precision_momentum: 0.99

  # Attention bias
  use_local_attention_bias: true

  # Adapter
  vocab_size: 11

training:
  epochs: 20000
  batch_size: 64
  learning_rate: 7e-5
  weight_decay: 1.0
  betas: [0.9, 0.95]
  gradient_clip: 1.0
  scheduler: cosine
  warmup_steps: 500

  # Mixed precision
  precision: bfloat16
  loss_precision: float64

  full_inner_backprop: true

  # Evaluation
  eval_every: 500
  pareto_eval_every: 5000
  quick_eval_samples: 100
  log_every: 50

  optimizer: adamw

  compile_model: false
  compile_dynamic: false

  # Codebook initialisation from Phase 1 collected states (optional)
  codebook_init_from: null

  # Amortisation annealing (disabled in phase 3)
  amort_anneal_start: 0
  amort_anneal_end: 0

data:
  dataset: sudoku_extreme_1k
  dataset_path: data/sudoku_extreme_1k
  augmentation_factor: 1000
  eval_size: 1000
  num_workers: 4
  seq_len: 81

wandb:
  project: "Sudoku-extreme-1k-aug-1000 CORAL-v4"
  entity: aktuator-ai
  run_name: v4.2-phase3a-crystal-simple
  tags: ["v4.2", "phase3a", "full", "crystallisation", "monolithic-codebook"]
  log_every: 50
  disabled: false

seed: 42
device: cuda
experiment_name: phase3a_crystal_simple
